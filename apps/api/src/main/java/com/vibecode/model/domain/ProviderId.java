package com.vibecode.model.domain;

/** Known model vendors. Adding one must never require changes outside the model module. */
public enum ProviderId {
  ANTHROPIC,
  OPENAI,
  GOOGLE,
  DEEPSEEK,
  OTHER
}
